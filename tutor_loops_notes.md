suggested loop(s) for arrays:
- normal for loop
for (let i =0; i < 10; i++) {}

suggested loop(s) for objects:
- for in loop
for (let key in object) {

}

for of loop- looping through the objects of the arrays
for(const r of obj[key]){}

for in and for of do the same "thing"

for in - goes through the keys of objects
for of - goes through the values of an array (directly gets the elements of an array)

for loop - array[index]
for of - directly accesses each element of the array
        - it can also traverse through string and output individual characters of the string